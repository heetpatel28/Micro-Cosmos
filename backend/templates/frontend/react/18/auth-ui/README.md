# {{SERVICE_NAME}}

Authentication UI - Generated by Microservice Generator

## Overview

This is a ready-to-run React authentication UI built with React 18, TypeScript, and Vite. It provides login, registration, and protected dashboard pages.

## Features

- Login page
- Registration page
- Protected dashboard route
- JWT token management
- Zustand state management
- React Router navigation
- Axios API integration

## Quick Start

### Prerequisites

- Node.js 18+ 
- npm or yarn

### Installation

```bash
npm install
```

### Configuration

Create `.env` file:

```env
VITE_API_URL={{DB_URL}}
```

Update the API URL to point to your backend authentication service.

### Development

```bash
npm run dev
```

### Production

```bash
npm run build
npm run preview
```

## Project Structure

```
src/
├── pages/
│   ├── LoginPage.tsx
│   ├── RegisterPage.tsx
│   └── DashboardPage.tsx
├── services/
│   └── api.ts
├── store/
│   └── authStore.ts
├── App.tsx
└── main.tsx
```

## Features

- **Login/Register**: Full authentication flow
- **Protected Routes**: Dashboard requires authentication
- **Token Management**: Automatic token storage and usage
- **State Management**: Zustand for auth state

## Integration

This UI connects to a backend authentication API at `VITE_API_URL`. The backend should provide:

- `POST /api/auth/login`
- `POST /api/auth/register`

## Notes

- Tokens are stored in localStorage
- Automatic redirect to login if not authenticated
- API requests include Bearer token automatically

---

Generated for domain: {{DOMAIN}}
Stack: {{STACK}} {{VERSION}}

