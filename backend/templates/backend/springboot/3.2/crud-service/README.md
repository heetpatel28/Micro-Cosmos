# {{SERVICE_NAME}}

CRUD microservice - Generated by Microservice Generator

## Overview

This is a ready-to-run CRUD microservice built with Spring Boot 3.2 and Java 17. It provides a complete REST API for managing items with PostgreSQL.

## Features

- Full CRUD operations (Create, Read, Update, Delete)
- PostgreSQL integration with Spring Data JPA
- Input validation
- Health check endpoint
- Docker support

## Quick Start

### Prerequisites

- Java 17 or higher
- Maven 3.6+
- PostgreSQL

### Installation

```bash
mvn clean install
```

### Configuration

Configure in `src/main/resources/application.properties`:

```properties
server.port={{PORT}}
spring.datasource.url={{DB_URL}}
```

### Development

```bash
mvn spring-boot:run
```

### Production

```bash
mvn clean package
java -jar target/{{SERVICE_NAME}}-1.0.0.jar
```

## API Endpoints

### Health Check

```
GET /health
```

### Items API

```
GET    /api/items        - Get all items
GET    /api/items/:id    - Get item by ID
POST   /api/items        - Create new item
PUT    /api/items/:id    - Update item
DELETE /api/items/:id    - Delete item
```

### Example Request

```bash
# Create item
curl -X POST http://localhost:{{PORT}}/api/items \
  -H "Content-Type: application/json" \
  -d '{"name": "Example Item", "description": "Item description", "price": 29.99}'

# Get all items
curl http://localhost:{{PORT}}/api/items
```

## Docker

Build and run with Docker:

```bash
docker build -t {{SERVICE_NAME}} .
docker run -p {{PORT}}:{{PORT}} -e DB_URL={{DB_URL}} {{SERVICE_NAME}}
```

## Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| PORT | Server port | {{PORT}} |
| DB_URL | Database URL | jdbc:postgresql://localhost:5432/{{SERVICE_NAME}} |
| DB_USERNAME | Database username | postgres |
| DB_PASSWORD | Database password | postgres |
| ENV | Environment | development |

---

Generated for domain: {{DOMAIN}}
Stack: {{STACK}} {{VERSION}}

